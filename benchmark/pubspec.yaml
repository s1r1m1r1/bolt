name: benchmark
description: benchmark state management .
version: 0.1.0

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  bloc: ^9.0.0
  riverpod: ^2.6.0
  state_notifier:
  mobx: ^2.2.1
  flutter:
    sdk: flutter
  bloc_bolt:
    path: ../

dev_dependencies:
  bloc_lint: ^0.3.2
  mocktail: ^1.0.0
  test: ^1.18.2
